# 灰空薄荷 · 部署教程

跟着做，大概 10 分钟就能把博客挂到 `https://hzhonoka.github.io/` 上。

## 1. 建仓库

1. 登录 GitHub，右上角 `+` → `New repository`
2. 仓库名**必须**填：`hzhonoka.github.io`（和你的用户名一模一样）
3. 选 Public，不用勾选 README，点 Create

## 2. 上传文件

把本文件夹里的**所有文件**（index.html、assets、data、posts 等，注意不是把外层文件夹传上去，是里面的内容）传到仓库里。

两种方式任选：

**方式 A：网页直接拖（最简单）**
在仓库页面点 `Add file` → `Upload files`，把所有文件和文件夹拖进去，提交即可。

**方式 B：git 命令行**

```bash
git init
git remote add origin https://github.com/hzhonoka/hzhonoka.github.io.git
git add .
git commit -m "小站开张"
git branch -M main
git push -u origin main
```

## 3. 确认上线

仓库名叫 `用户名.github.io` 时 GitHub Pages 会自动开启，等 1~3 分钟，访问：

```
https://hzhonoka.github.io/
```

就能看到你的博客了。以后每次改完文件 push 上去，约 1 分钟后网站自动更新。

## 4. 开启评论（Giscus）

1. 仓库页面 → `Settings` → 左侧 `General` 往下拉，找到 `Features`，勾选 **Discussions**
2. 打开 <https://github.com/apps/giscus> 点 Install，授权给 `hzhonoka.github.io` 这个仓库
3. 打开 <https://giscus.app/zh-CN>，在"仓库"一栏填 `hzhonoka/hzhonoka.github.io`，页面下方会生成一段配置
4. 从配置里复制 `data-repo-id` 和 `data-category-id` 两个值，填进 `data/config.js` 里对应位置：

```js
window.GISCUS_CONFIG = {
  repo: "hzhonoka/hzhonoka.github.io",
  repoId: "粘贴到这里",
  category: "General",
  categoryId: "粘贴到这里",
};
```

5. push 上去，评论就生效了。访客点任意动态/文章下的"评论"，用 GitHub 账号登录即可留言。

## 日常使用

| 想做的事 | 改哪里 |
| --- | --- |
| 发碎碎念 | `data/moments.js` 最上面加一段 |
| 发文章 | `posts/` 里新建 `.md` 文件 + `data/posts.js` 登记一行 |
| 加照片 | 图片放进 `assets/photos/` + `data/photos.js` 加一条 |
| 换头像 | 图片覆盖到 `assets/` 并在 `data/config.js` 改 avatar 路径 |
| 改昵称/签名 | `data/config.js` |
| 改自我介绍 | `about.html`（里面都是中文，直接改字） |
| 换强调色 | 改 `assets/style.css` 最上面 `--sage` 变量的色值 |

每个数据文件里都有注释教你怎么加内容，照抄格式就行，**注意别漏逗号**。
